# Development — minimal single-stage image.
# Source code is mounted as a volume; Vite dev server hot-reloads on change.

FROM node:20-alpine

WORKDIR /app

COPY . .
RUN npm install

EXPOSE 3000

# Source mounted at runtime via docker-compose volumes.
CMD ["npm", "run", "dev"]
